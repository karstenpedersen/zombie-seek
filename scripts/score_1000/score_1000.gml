function score_1000() {
	if global.debug_mode_ = true {
		global.score_ += 1000;
		log += chr(13) + "Added 1000 to SCORE"
	} else {
		log += chr(13) + "Type cheats_enable to enable cheats"
	}


}
